workflows:
  android-build:
    name: Android Build
    instance_type: mac_mini_m1
    working_directory: rabbit_coin_app # Set the working directory for the entire workflow
    environment:
      flutter: stable
      xcode: 15.0.x
      cocoapods: 1.12.1
      java: 17
    scripts:
      - name: Set up Flutter
        script: |
          flutter clean
          flutter pub get
      - name: Build Android Release
        script: |
          flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*.apk


